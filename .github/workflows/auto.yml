name: auto

on: push
jobs:
  wget:
    runs-on: ubuntu-latest
    steps:
    - name: wget
      uses: wei/wget@v1
      with:
        args: https://raw.githubusercontent.com/Loyalsoldier/surge-rules/release/proxy.txt -nv -O proxy.list
