name: Download file
on:
  schedule:
    - cron: '16 13 * * *'
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Download file
      run: |
        curl -o /list/proxy.list https://raw.githubusercontent.com/Loyalsoldier/surge-rules/release/proxy.txt
